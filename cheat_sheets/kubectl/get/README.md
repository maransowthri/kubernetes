# `kubectl get` Commands

## List all objects

```bash
kubectl get all
```

## List all pods / replicasets / deployments and watch it live

```bash
kubectl get pods / replicasets / deployments -w
```
